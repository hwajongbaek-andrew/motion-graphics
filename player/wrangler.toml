# Cloudflare Workers config — deploys the Vite build in ./dist as a static site.
# No Worker script; assets-only = pure CDN delivery.
name = "motion-graphics"
compatibility_date = "2024-12-01"

[assets]
directory = "./dist"
not_found_handling = "single-page-application"
